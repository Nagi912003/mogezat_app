import 'package:flutter/material.dart';

class LangsScreen extends StatelessWidget {
  const LangsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('LangsScreen'),
      ),
    );
  }
}
